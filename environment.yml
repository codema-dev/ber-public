name: base
channels:
  - conda-forge
  - defaults
dependencies:
  - conda
  - python=3.8
  - geopandas
  - poetry
  - ipython
  - requests
  - dask
  - pyarrow
  - tqdm
prefix: /home/wsl-rowanm/miniconda3
